// 1 - Crie um array de números e utilize o método map para retornar um novo array com todos os números dobrados.

const numbers = [1, 2, 3, 4, 5];

let resultado = numbers.map(number => number * 2)

console.log(resultado)
